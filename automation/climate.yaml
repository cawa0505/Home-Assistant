#################################################################
## Thermostat Automations
#################################################################


  #################################################################
  ## Thermostat offline
  #################################################################

    - alias: 'Thermostat offline'
      initial_state: on
      trigger:
        platform: state
        from: 'on'
        to: 'off'
        entity_id: binary_sensor.heating_thermostat_online

      action:
      - service: notify.telegram
        data:
         message: 'Thermostat is offline - please check heating'  
